---
import type { Locale } from '@/i18n/locale';
import { buildAlternateLocalePath, localesOtherThan } from '@/i18n/routes';
import { getTranslator } from '@/i18n/translator';

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;
const currentPathname = Astro.url.pathname;
---

<ul>
  {
    localesOtherThan(locale).map((target) => {
      const translateTarget = getTranslator(target);
      return (
        <li>
          <a href={buildAlternateLocalePath(currentPathname, locale, target)} lang={target}>
            {translateTarget('language.current')}
          </a>
        </li>
      );
    })
  }
</ul>
